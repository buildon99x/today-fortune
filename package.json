{
  "name": "toss-fortune-miniapp",
  "version": "0.1.0",
  "private": true,
  "description": "AI 운세 미니앱 (앱인토스) — 검증된 운세 코어 + RN 셸 스캐폴드",
  "scripts": {
    "test": "node --test",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev": "ait dev",
    "build": "ait build",
    "deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/framework": "latest",
    "react": "*",
    "react-native": "*"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "eslint": "^9.39.4",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.6.0",
    "prettier": "^3.8.3",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.60.0"
  },
  "comment": "react/react-native/@apps-in-toss/framework 버전은 `ait init`이 생성하는 값으로 맞추세요. test 스크립트만 이 환경에서 검증됨."
}
