# Claude Code 작업 지침

이 저장소에서 작업하기 전 **반드시 `AGENTS.md`를 먼저 읽어라**. 모든 코딩 규칙·프라이버시 invariant·아키텍처 규약·안티패턴이 거기에 있다.

핵심 한 줄: **DB 없음 / PII 휘발 / 모든 식별자는 `hashIdentifier()` 통과 / production 가드 우회 금지.**

→ [`AGENTS.md`](./AGENTS.md)
